<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for val in values %}
    <experiment>{% for key, value in val.items() %}<{{ key|safe }}>{{ value|safe }}</{{ key|safe }}>{% endfor %}</experiment>
    {% endfor %}
</urlset>